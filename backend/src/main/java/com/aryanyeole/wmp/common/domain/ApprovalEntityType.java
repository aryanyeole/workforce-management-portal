package com.aryanyeole.wmp.common.domain;

/** Mirrors the CHECK constraint on approval_events.entity_type. */
public enum ApprovalEntityType {
    EXPENSE_REPORT,
    PAYROLL_RUN,
    ONBOARDING_TASK
}
