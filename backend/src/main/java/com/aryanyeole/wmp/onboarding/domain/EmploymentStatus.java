package com.aryanyeole.wmp.onboarding.domain;

/** Mirrors the CHECK constraint on employees.employment_status. */
public enum EmploymentStatus {
    PENDING,
    ACTIVE,
    ON_LEAVE,
    TERMINATED
}
